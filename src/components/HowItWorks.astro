---
const steps = [
  {
    number: '01',
    title: 'Pilih Layanan',
    description: 'Buka website Mimio dan pilih layanan yang kamu butuhkan (M-Food, M-Send, atau M-Mart).',
    icon: '📱'
  },
  {
    number: '02',
    title: 'Chat Mimin',
    description: 'Klik tombol pesan, kamu akan diarahkan ke WhatsApp mimin kami. Tulis pesananmu.',
    icon: '💬'
  },
  {
    number: '03',
    title: 'Duduk Santai',
    description: 'Driver kami akan segera memproses pesananmu dan mengantarkannya sampai ke depan rumah.',
    icon: '🛵'
  }
];
---

<section id="how-it-works" class="py-20 md:py-32 bg-white relative overflow-hidden">
  <div class="container mx-auto px-4 relative z-10">
    <div class="text-center mb-16 md:mb-24 animate-prepare">
      <div class="inline-block bg-[var(--color-primary-400)] text-black px-4 py-1.5 rounded-full text-sm font-bold mb-6 border border-black/5 shadow-sm">
        Cara Pesan
      </div>
      <h2 class="font-display text-4xl md:text-5xl font-extrabold text-black mb-6 tracking-tight">
        Gampang Banget!
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto leading-relaxed">
        Cuma butuh 3 langkah simpel buat dapetin apa yang kamu mau tanpa ribet install aplikasi berat.
      </p>
    </div>

    <div class="relative grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">
      <!-- Decorator Line (Desktop) -->
      <div class="hidden md:block absolute top-12 left-0 w-full h-0.5 bg-gradient-to-r from-transparent via-[var(--color-primary-400)] to-transparent -z-10"></div>

      {steps.map((step, index) => (
        <div class={`relative flex flex-col items-center text-center animate-prepare delay-${index * 100 + 100}`}>
          <div class="w-24 h-24 rounded-full bg-white border-4 border-[var(--color-primary-400)] flex items-center justify-center text-4xl mb-8 shadow-[0_10px_30px_rgba(255,239,0,0.3)] z-10 relative group">
            <span class="transform group-hover:scale-110 transition-transform duration-300 block">{step.icon}</span>
            <div class="absolute -top-3 -right-3 w-8 h-8 rounded-full bg-black text-white flex items-center justify-center font-bold text-sm border-2 border-white">
              {step.number}
            </div>
          </div>
          <h3 class="font-display text-2xl font-bold text-black mb-4">{step.title}</h3>
          <p class="text-gray-600 leading-relaxed max-w-xs">
            {step.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>
